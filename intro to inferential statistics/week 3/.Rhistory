library(statsr)
library(dplyr)
library(ggplot2)
data(nc)
head(nc)
str(nc)
summary(nc$gained)
# type your code for the Question 3 here, and Knit
ggplot(nc, aes(x=factor(habit), y=weight)) +
geom_boxplot()
nc %>%
group_by(habit) %>%
summarise(mean_weight = mean(weight))
nc %>%
group_by(habit) %>%
summarise(mean_weight = n()))
help(n)
nc %>%
group_by(habit) %>%
summarise(mean_weight = n())
inference(y = weight, x = habit, data = nc, statistic = "mean", type = "ht", null = 0,
alternative = "twosided", method = "theoretical")
inference(y = weight, x = habit, data = nc, statistic = "mean", type = "ci", null = 0,
alternative = "twosided", method = "theoretical")
inference(y = weight, x = habit, data = nc, statistic = "mean", type = "ht", null = 0,
alternative = "twosided", method = "theoretical")
# type your code for the Question 5 here, and Knit
inference(y= weight, x = habit, data=nc, statistic = "mean", type = "ci", conf_level = 0.95)
# type your code for the Question 5 here, and Knit
inference(y= weight, x = habit, data=nc, statistic = "mean", type = "ci", conf_level = 0.95, method = "theoretical")
inference(y = weight, x = habit, data = nc, statistic = "mean", type = "ci",
method = "theoretical", order = c("smoker","nonsmoker"))
# type your code for Question 6 here, and Knit
inference(y = weeks, data = nc, statistic = "mean", type = "ci",
method = "theoretical", conf_level = 0.99)
# type your code for the Exercise here, and Knit
inference(y = weeks, data = nc, statistic = "mean", type = "ci",
method = "theoretical", conf_level = 0.90)
str(nc)
# type your code for the Exercise here, and Knit
inference(y = weeks, x = mature, statistic = "mean", type = "ht",
method="theoretical", order = c("mature mom", "younger mom"))
# type your code for the Exercise here, and Knit
inference(y = weeks, x = mature, data = nc statistic = "mean", type = "ht",
# type your code for the Exercise here, and Knit
inference(y = weeks, x = mature, data = nc, statistic = "mean", type = "ht",
method="theoretical", order = c("mature mom", "younger mom"))
# type your code for the Exercise here, and Knit
inference(y = weeks, x = mature, data = nc, statistic = "mean", type = "ht",
null = 0, method="theoretical", order = c("mature mom", "younger mom"))
# type your code for the Exercise here, and Knit
inference(y = weeks, x = mature, data = nc, statistic = "mean", type = "ht",
null = 0, alternative = "twosided", method="theoretical", order = c("mature mom", "younger mom"))
str(nc)
# type your code for Question 7 here, and Knit
nc %>%
group_by(mature) %>%
summarise(mean_weight = mean(mage))
# type your code for Question 7 here, and Knit
nc %>%
group_by(mature) %>%
summarise(mean_weight = max(mage))
# type your code for Question 7 here, and Knit
nc %>%
group_by(mature) %>%
summarise(max_age = max(mage), min_age = min(mage))
# type your code for the Exercise here, and Knit
inference(y = visits, x = mature, data = nc, statistic = "mean", type = "ht",
null = 0, method = "twosided", type="theoretical", order = c("mature mom", "younger mom"))
# type your code for the Exercise here, and Knit
inference(y = visits, x = mature, data = nc, statistic = "mean", type = "ht",
null = 0, alternative = "twosided", order = c("mature mom", "younger mom"))
# type your code for the Exercise here, and Knit
inference(y = visits, x = mature, data = nc, statistic = "mean", type = "ht",
null = 0, alternative = "twosided", method = "theoretical", order = c("mature mom", "younger mom"))
